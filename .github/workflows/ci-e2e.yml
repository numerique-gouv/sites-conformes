name: 🎭 CI - E2E tests

on: [push, pull_request]

jobs:
  e2e:
    name: Baseline (current branch)
    uses: ./.github/workflows/_e2e.yml
    with:
      ref: ${{ github.ref }}
      compare: false
    permissions:
      contents: read

permissions:
  pull-requests: write
  contents: read
